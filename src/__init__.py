# Empty on purpose: this file exists only so stdlib unittest discovery
# (python3 -m unittest discover) can import src.queue / src.worker / src.cli
# as a package on the 3.9 floor this repo declares.
